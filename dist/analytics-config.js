// Edite apenas este arquivo depois de publicar o backend.
// Se o Worker estiver no MESMO domínio em /api, não precisa alterar nada.
window.__LOVE_ANALYTICS__ = {
  enabled: true,
  apiBase: '/api'
};
