PATCH — LOADING RÁPIDO + TRAVA DE SCROLL

Correções:
- não espera document.fonts.ready (o projeto usa fontes do sistema);
- não decodifica as mesmas imagens duas vezes;
- Hero recebe prioridade alta;
- "Nosso primeiro beijo" carrega junto;
- scroll/swipe/teclas ficam bloqueados enquanto o loader está na tela;
- a página é forçada para o topo antes e depois do loading;
- 100% só aparece quando Hero + Primeiro Beijo + app estão prontos;
- se um asset crítico falhar por 15 s, não finge 100%: mostra "Recarregue a página".

Arquivo alterado:
- index.html
